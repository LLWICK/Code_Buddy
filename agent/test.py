from states import *

arr = [TaskPlan(implementation_steps=[ImplementationTask(filepath='package.json', task_description='Create the project manifest. Define name, version, description, main entry (src/app.js), scripts: `start` (serve via simple http server or node), `test` (jest), `lint` (eslint), `build` (if needed). Add dependencies: none for runtime, devDependencies: jest, eslint, @babel/core, @babel/preset-env, jest-environment-jsdom, nodemon (optional). Include `type: module` to enable ES6 imports. This file is required before any source files can be imported or tested.'), ImplementationTask(filepath='src/calculator.js', task_description='Implement the core calculation engine.\n- Export a class `Calculator` with a public method `evaluate(expression: string): number`.\n- Inside, implement private helper functions: `tokenize(expr)`, `toRPN(tokens)`, `computeRPN(rpnTokens)`.\n- Support basic operators (+, -, *, /) with correct precedence and associativity.\n- Add scientific functions: sin, cos, tan, log, ln, sqrt, pow, parentheses handling.\n- Throw descriptive errors for syntax issues.\n- Export utility constants `OPERATORS` and `FUNCTIONS` for use by other modules (e.g., UI for button generation).\n- Include JSDoc comments for type hints.\n- This module is a dependency for `src/app.js` and `src/history.js` (if history stores raw expressions).'), ImplementationTask(filepath='src/history.js', task_description='Create a history management module.\n- Export a class `History` with methods:\n  * `addEntry(expression: string, result: number): void`\n  * `clear(): void`\n  * `deleteEntry(index: number): void`\n  * `getEntries(): Array<{expression:string,result:number}>`\n  * `render(container: HTMLElement): void` – renders the list into the supplied container, attaching delete buttons.\n- Persist entries in `localStorage` under key `webcalc_history` and load them on construction.\n- Use the `Calculator` class only for type consistency (no evaluation needed).\n- This module will be imported by `src/app.js` to update UI and by the history panel in `index.html`.'), ImplementationTask(filepath='src/styles.css', task_description='Define the visual styling for the calculator.\n- Create CSS custom properties for light and dark themes (`--bg-color`, `--text-color`, `--button-bg`, etc.).\n- Write a `.dark-theme` class that overrides these variables.\n- Implement a responsive grid layout: a display area, button grid (4 columns on desktop, 2‑3 columns on mobile), and a side panel for history that collapses into a bottom drawer on small screens.\n- Add transition effects for theme toggle.\n- Ensure button states (hover, active) are styled.\n- This stylesheet will be linked in `public/index.html` and referenced by `src/app.js` for theme toggling.'), ImplementationTask(filepath='public/index.html', task_description='Create the main HTML skeleton.\n- Include `<link rel="stylesheet" href="../src/styles.css">`.\n- Add a root `<div id="calculator">` containing:\n  * `<input id="display" type="text" readonly>` for the current expression/result.\n  * `<div id="buttons" class="button-grid">` with data‑attributes for each operation (e.g., `data-value="+"`). Buttons for numbers 0‑9, basic ops, scientific functions, parentheses, clear, delete, equals.\n  * `<button id="theme-toggle">` for light/dark mode.\n- Add a `<section id="history-panel" class="history">` where the `History.render` method will inject entries.\n- Include `<script type="module" src="../src/app.js"></script>` at the end of body.\n- This file depends on the CSS and JS modules defined earlier.'), ImplementationTask(filepath='src/app.js', task_description="Implement the frontend logic tying UI, calculator engine, and history.\n- Import `{ Calculator }` from `./calculator.js` and `{ History }` from `./history.js`.\n- Instantiate `const calc = new Calculator(); const history = new History();`.\n- Cache DOM elements: display input, button container, history panel, theme toggle.\n- Attach click listeners to all buttons using event delegation (`buttons.addEventListener('click', ...)`). On click, update the display string or call `calc.evaluate` when `=` is pressed.\n- Implement keyboard support: `document.addEventListener('keydown', ...)` mapping keys to button actions (numbers, operators, Enter for evaluate, Backspace for delete, etc.).\n- After a successful evaluation, call `history.addEntry(currentExpression, result)`, then `history.render(historyPanel)`.\n- Implement clear (`C`) and delete (`←`) functionalities.\n- Theme toggle: on click, toggle `document.body.classList.toggle('dark-theme')` and store preference in `localStorage`.\n- Export a function `init()` that runs the above setup; call it at the end of the module.\n- This file depends on `calculator.js`, `history.js`, and the DOM defined in `index.html`."), ImplementationTask(filepath='.github/workflows/ci.yml', task_description='Configure GitHub Actions CI pipeline.\n- Trigger on `push` and `pull_request` for `main`.\n- Jobs: `build` using `ubuntu-latest`.\n- Steps: checkout repository, set up Node (`actions/setup-node@v3` with version 20), cache `node_modules`, run `npm ci`, run `npm run lint` (eslint), run `npm test` (jest), optionally run a simple build step (`npm run build` if defined).\n- Upload test results as artifacts.\n- This workflow relies on scripts defined in `package.json`.'), ImplementationTask(filepath='README.md', task_description='Write project documentation.\n- Sections: Overview, Features, Tech Stack, Getting Started (clone, `npm install`, `npm start`), Testing (`npm test`), CI status badge, Usage (keyboard shortcuts, theme toggle), Contributing, License.\n- Include screenshots of light/dark UI and responsive layout.\n- Reference the CI workflow badge and link to the live demo (if any).\n- This file provides context for users and developers after all code is in place.')], plan=Plan(name='WebCalc', description='A responsive web-based calculator application that performs basic and scientific calculations with a clean UI, keyboard support, and calculation history.', techstack='javascript, html, css, node, npm, jest, github-actions', features=['Basic arithmetic operations (addition, subtraction, multiplication, division)', 'Scientific functions (trigonometry, logarithms, exponentiation, parentheses)', 'Responsive design for desktop and mobile browsers', 'Keyboard input support for all operations', 'Calculation history panel with clear and delete options', 'Theme toggle (light/dark mode)', 'Unit tests for core calculation logic', 'Continuous integration setup with GitHub Actions'], files=[File(path='public/index.html', purpose='Main HTML page containing the calculator layout and UI elements'), File(path='src/styles.css', purpose='Styling for the calculator, including responsive layout and theme support'), File(path='src/app.js', purpose='Frontend JavaScript handling UI interactions, keyboard input, and calculation logic'), File(path='src/calculator.js', purpose='Core calculation engine (parsing and evaluating expressions) with unit tests'), File(path='src/history.js', purpose='Module managing calculation history storage and rendering'), File(path='package.json', purpose='Project metadata, dependencies, and scripts for building/testing'), File(path='.github/workflows/ci.yml', purpose='GitHub Actions workflow for linting, testing, and building the app'), File(path='README.md', purpose='Project documentation, setup instructions, and usage guide')]))]

print(arr[0].get("code_state"))

#print(len(arr[0].implementation_steps))